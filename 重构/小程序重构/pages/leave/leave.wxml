<!--pages/leave/leave.wxml-->
<view class="page-container" style="padding-top: {{navBarFullHeight}}px;">

  <!-- 透明状态栏 -->
  <view class="liquid-status-bar" style="height: {{navBarFullHeight}}px;">
    <view class="status-inner" style="margin-top: {{navTop}}px; height: {{navHeight}}px;">
      <view class="back-btn" bindtap="goBack">‹ Back</view>
    </view>
  </view>

  <block wx:if="{{isVip}}">
    <view class="container fade-in">
      <view class="header-section">
        <text class="display-title">{{L.tag}}</text>
        <text class="title">{{L.title}}</text>
        <text class="subtitle">{{L.subtitle}}</text>
      </view>

      <view class="form-container">
        <view class="form-group">
          <text class="form-label">{{L.fieldStudent}}</text>
          <picker bindchange="bindStudentChange" value="{{studentIdx}}" range="{{students}}" range-key="name">
            <view class="form-input">
              {{students.length > 0 ? students[studentIdx].name : '...'}}
            </view>
          </picker>
        </view>

        <view class="form-group">
          <text class="form-label">{{L.fieldDate}}</text>
          <picker mode="date" value="{{date}}" bindchange="bindDateChange">
            <view class="form-input">{{date}}</view>
          </picker>
        </view>

        <view class="form-group form-group-textarea">
          <text class="form-label">{{L.fieldReason}}</text>
          <textarea 
            class="form-input textarea" 
            placeholder="{{L.phReason}}" 
            placeholder-class="plas"
            bindinput="handleReason"
            maxlength="200"
          ></textarea>
          <view class="word-count">{{reason.length}}/200</view>
        </view>

        <view class="form-btn-wrap">
          <button class="btn-black" bindtap="submitLeave">SUBMIT</button>
        </view>
      </view>
    </view>
  </block>

  <view wx:else class="container" style="display:flex;align-items:center;justify-content:center;height:80vh;">
    <text style="color:#ccc">Coming Soon</text>
  </view>

</view>
